// Inherit the parent event
event_inherited();

damage = 20;
enemyDestroy = false;
alarm[0] = 60; // this is how long the sprite stays on screen before disappearing

//TODO: set the "visible" property to false